import 'package:flutter/material.dart';

class AppColors {
  // Warna Utama dari Project Doc
  static const Color primary = Color(0xFF2E7D32);   // Hijau segar
  static const Color secondary = Color(0xFFF9A825); // Kuning warning/confidence
  static const Color error = Color(0xFFC62828);     // Merah alert/error
  
  // Background & Surface
  static const Color surface = Color(0xFFFAFAFA);   // Background halaman
  static const Color card = Color(0xFFFFFFFF);      // Background card
  
  // Teks
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
}